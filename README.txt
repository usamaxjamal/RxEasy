RxEasy split package

What changed:
- Inline JavaScript was extracted into assets/js/*.js files.
- The HTML files were left otherwise unchanged to minimize behavior changes.
- External CDN dependencies remain exactly as in the original pages.

Important:
- This package preserves the original client-side behavior.
- It does not fix backend security issues by itself.
